<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/viewPadding">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:showDividers="middle"
        android:divider="@drawable/empty_divider">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_general_title"
            android:textAppearance="@style/Widget.PlainContainers.Text.Title.Bold"
        />

        <Button
            android:id="@+id/btnSettingsLanguage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/settings_general_language"
            android:textAppearance="@style/Widget.PlainContainers.Text.Button"
            android:gravity="start|center"
            android:padding="0dp"
        />
    </LinearLayout>

</ScrollView>